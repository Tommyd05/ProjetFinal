int playerSize = 20;

void setup(){
    size(1000,1000);
    background(240);
    noCursor();
    intro();
}
void draw(){
  background(240);
  /**joueur*/
  circle(mouseX,mouseY, playerSize);
    
}
void intro(){ 
  
}
